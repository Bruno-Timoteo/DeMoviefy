# Single venv for the whole repository (run from repo root)

python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r demoviefy-backend/requirements.txt

# GUI launcher for the whole project
python run_form.py

# Run the MVC YOLO test app
python teste/app/app.py

# Optional: analyze a specific video path
$env:TEST_VIDEO_PATH="uploads\your_video.mp4"
python teste/app/app.py
